sudo: false
language: julia
julia:
 - 1.0
# - nightly
before_install:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
script:
  - julia -e 'versioninfo(); Pkg.init(); Pkg.clone(pwd()); Pkg.build("ROCAnalysis")'
  - xvfb-run julia --check-bounds=yes -e 'Pkg.test("ROCAnalysis", coverage=true)'
